
</div>
<form class="form-add-produc" action="/agregar_producto" method="POST">
    <div class="mb-4 text-center">
        <h1>Productos</h1>
    </div>
       <div class="container">
           <h2 class="text-center"><label> {{producto.name}} </label></h2>
            <section class="form-add-products">
      <input class="controls" type="text" name="name"   id="name"   placeholder="Nombre" value="{{producto.name}} "  required>
      <input class="controls" type="text" name="price"   id="price"   placeholder="Precio" value="{{producto.price}}" required>
      <input class="controls" type="text" name="quantity"   id="quantity"   placeholder="Cantidad" value="{{producto.quantity}}" required>
      <input class="controls" type="text" name="description"   id="description"   placeholder="Descripción" value="{{producto.description}}" required>
      <input class="controls" type="file" name="image"   id="image"   placeholder="Imagen" value="{{producto.image}}"  required>
     
      <button class="buttons" type="submit">Agregar Producto</button>
    </section>
       </div>
</form>

<div class="container text-center container-products">
    
{{#if productos}}
<h1>Productos</h1>
   {{#each productos}}

        <div class="card card-view-products" style="width: 18rem;" >
        <div class="card-body">
            <p class="card-text">{{dataValues.idProducto}}</p>
            <p class="card-text">{{dataValues.image}}</p>
            <p class="card-text">{{dataValues.name}}</p>
            <p class="card-text">{{dataValues.price}}</p>
            <p class="card-text">{{dataValues.quantity}}</p>
            <p class="card-text">{{dataValues.description}}</p>
             <img src=" {{producto.image}} " alt="">
            <a class="buttons" href="/modificar_producto/{{dataValues.url}}" >Modificar</a>
                    <button class="buttons" name="eliminar-producto" id="eliminar-producto" type="button" data-producto-url="{{dataValues.url}}">Eliminar</button>
    </div>
    </div>
    {{/each}}
{{/if}}
